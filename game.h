#ifndef GAME_H
#define GAME_H

/*
 * game.h
 *
 * Purpose:
 * High-level game-state boundary for the CSC209 A3 server.
 * The server will own authoritative game state, while this header will expose
 * only the public structs/enums/functions needed by networking code.
 *
 * Current scope:
 * Skeleton only. The exact public interface is intentionally left open until
 * the lobby protocol and phase flow are specified in more detail.
 *
 * Likely future helpers, not finalized:
 * - game_state_t definition
 * - game_phase_t enum
 * - public state transition helpers used by server.c
 */

#include "protocol.h"

#endif
