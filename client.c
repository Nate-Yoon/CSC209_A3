/*
 * client.c
 *
 * Purpose:
 * Terminal client for the CSC209 A3 multiplayer game.
 * This file will eventually connect to the authoritative server, send lobby
 * messages such as JOIN and READY, and display server responses to the user.
 *
 * Current scope:
 * Skeleton only. No socket connection or stdin/socket event handling exists
 * yet.
 *
 * Likely future helpers, not finalized:
 * - TCP connection setup
 * - line-oriented send/receive helpers
 * - stdin/socket interaction loop
 * - lobby command handling for JOIN and READY
 */

#include "client.h"

int main(void) {
    return 0;
}
