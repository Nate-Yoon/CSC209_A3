#ifndef CLIENT_H
#define CLIENT_H

/*
 * client.h
 *
 * Purpose:
 * Public client-facing declarations for the CSC209 A3 project.
 * This header will eventually hold the small stable interface needed by the
 * client implementation and any shared client-side state definitions.
 *
 * Current scope:
 * Skeleton only. No client interface is finalized yet.
 *
 * Likely future helpers, not finalized:
 * - client state struct
 * - top-level client startup/run entry point
 * - display or input helper interfaces if they become reusable
 */

#include "protocol.h"

#endif
