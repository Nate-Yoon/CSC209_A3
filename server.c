/*
 * server.c
 *
 * Purpose:
 * Main authoritative server for the CSC209 A3 multiplayer terminal game.
 * This file will eventually own the listening socket, client acceptance,
 * select()-based concurrency loop, disconnect handling, and lobby flow.
 *
 * Current scope:
 * Skeleton only. No socket setup or event loop has been implemented yet.
 *
 * Likely future helpers, not finalized:
 * - listening socket setup
 * - client slot management
 * - select() loop / fd-set maintenance
 * - JOIN and READY lobby handling
 * - broadcast and disconnect cleanup helpers
 */

#include "server.h"

int main(void) {
    return 0;
}
